import { TitulosDirectiveDirective } from './titulos-directive.directive';

describe('TitulosDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TitulosDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
