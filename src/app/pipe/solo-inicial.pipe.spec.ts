import { SoloInicialPipe } from './solo-inicial.pipe';

describe('SoloInicialPipe', () => {
  it('create an instance', () => {
    const pipe = new SoloInicialPipe();
    expect(pipe).toBeTruthy();
  });
});
